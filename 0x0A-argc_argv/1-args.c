#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * main - prints the number of arguments passed to the cl
 * @argc: first param
 * @argv: second param
 *
 */
int main(int argc, char* argv[])
{
(void)argv;
printf("%d", argc);
printf("\n");
return (0);
}
