#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _print_rev_recursion - reverses a string
 * @s: param
 *
 * Return: reversed string
 */
void _print_rev_recursion(char *s)
{
puts(strrev(rev));
}
