#include "main.h"
#include <stdio.h>
/**
 * add - adds two interger
 * @a: first integer
 * @b: second integer
 * Return: returns sum of integers
 */
int add(int a, int b)
{
int result = a + b;
_putchar(result);
return (result);
}
