#include <stdio.h>
#include "main.h"
/**
 * main - run the code
 * don't fo
 * Return: alwasy return 0
 */
void print_alphabet(void)
{
char A;
for (A = 'a'; A <= 'z'; A++)
{
_putchar(A)
}
}
int main(void)
{
print_alphabet();
_putchar('\n');
return (0);
}
