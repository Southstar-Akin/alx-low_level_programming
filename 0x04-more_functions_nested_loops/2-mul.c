#include "main.h"
/**
 * mul - multiplies the two arguments given to program
 * @a: first argument
 * @b: second argument
 *
 * Return: returns the result of the multiplication
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
