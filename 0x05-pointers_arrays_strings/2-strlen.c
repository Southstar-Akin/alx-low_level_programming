#include "main.h"
/**
 * _strlen - returns the lenght of a string
 * @s: is the string input
 *
 * Return: returns the lenght of the string
 */
int _strlen(char *s)
{
int result;
result = strlen(*s);
return (result);
}
