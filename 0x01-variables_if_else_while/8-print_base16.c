#include <stdio.h>
/**
 * main - this na code
 * no even whine me
 * Return: returns 0 idf successful
 */
int main(void)
{
char C;
char d;
for (C = 0; C < 10; C++)
{
putchar(C);
}
for (d = 'a'; d < 'f'; d++)
{
putchar(d);
}
putchar('\n');
return (0);
}
