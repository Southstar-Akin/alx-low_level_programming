#include <stdio.h>
/**
 * main - this na code
 * no even whine me
 * Return: returns 0 idf successful
 */
int main(void)
{
char C;
char T;
for (C = 'a'; C <= 'z'; C++)
{
putchar(C);
}
for (T = 'A'; T <= 'Z'; T++)
{
putchar(T);
}
putchar('\n');
return (0);
}
