#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two string
 * @s1: first string
 * @s2: second string
 *
 * Return: returns an interger 0 <= x >= 0
 */
int _strcmp(char *s1, char *s2)
{
int res = strcmp(s1, s2);
return (res);
}
