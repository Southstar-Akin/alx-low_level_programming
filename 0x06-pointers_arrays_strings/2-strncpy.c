#include "main.h"
#include <string.h>
/**
 * _strncpy - copies a string
 * @dest: destination string
 * @src: source to copy valuues
 * @n: Number of characters to copy
 *
 * Return: dest
 */
char *_strncpy(char *dest, char *src, int n)
{
strncpy(dest, src n);
return (dest);
}
