#include <stdio.h>
/**
 * main - this is the function that runs printin the text specified
 * Return: 0 is returned if function is succesful
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
